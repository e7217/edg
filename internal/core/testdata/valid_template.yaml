name: test-sensor
description: Test sensor template for unit tests
resources:
  - name: temperature
    valueType: NUMBER
    unit: celsius
  - name: status
    valueType: TEXT
  - name: enabled
    valueType: FLAG
constraints:
  required_relations:
    - type: partOf
      target_template: equipment
      min: 1
      max: 1
