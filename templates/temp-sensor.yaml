name: temp-sensor
resources:
  - name: temperature
    valueType: NUMBER
    unit: "°C"
  - name: humidity
    valueType: NUMBER
    unit: "%"
constraints:
  required_relations:
    - type: partOf
      target_template: equipment
      min: 1
      max: 1
